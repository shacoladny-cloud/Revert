mysql:
  host: "127.0.0.1"
  port: 3306
  database: "minecraft"
  username: "root"
  password: ""
  pool:
    maximum-pool-size: 10
    connection-timeout: 5000

server:
  name: "lobby"
  is-lobby: false

redirect:
  delay-ticks: 20
  allowed-servers:
    - "build"
    - "farm"
    - "arts"

debug: false
